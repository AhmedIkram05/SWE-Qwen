# ── Model Registry ────────────────────────────────────────────────────────────
# Each entry maps a short name → Hugging Face model ID + hardware requirements.
# Used by training/qlora_config.py to resolve GPU type and model config.
#
# PHASE 4 PIVOT: Only qwen3-14b is used for Phase 4 training.
# qwen3-30b-a3b config retained for future phases but excluded from Phase 4 execution.

models:
  qwen3-14b:
    hf_id: "Qwen/Qwen3-14B"
    active_params: 14_000_000_000
    total_params: 14_000_000_000
    quantization: "nf4"
    compute_dtype: "bfloat16"
    gpu_mapping:
      primary: "a100-80gb"
      fallback: "a10g-24gb"
    context_window: 32768
    target_modules:
      - "q_proj"
      - "k_proj"
      - "v_proj"
      - "o_proj"
      - "gate_proj"
      - "up_proj"
      - "down_proj"

  # Retained for future phases (Phase 5+ re-evaluation, Phase 6+ serving)
  # NOT used in Phase 4 training pipeline
  qwen3-30b-a3b:
    hf_id: "Qwen/Qwen3-30B-A3B"
    active_params: 3_000_000_000
    total_params: 30_000_000_000
    quantization: "nf4"
    compute_dtype: "bfloat16"
    gpu_mapping:
      primary: "h100-80gb"
      fallback: "a100-80gb"
    context_window: 32768
    target_modules:
      - "q_proj"
      - "k_proj"
      - "v_proj"
      - "o_proj"
      - "gate_proj"
      - "up_proj"
      - "down_proj"
    phase4_excluded: true
